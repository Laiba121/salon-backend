Firebase Setup

Install Firebase Admin:

npm install firebase-admin

Add Firebase service key in `src/firebase-key.json`
Initialize Firebase in project and use `db` to read/write Firestore
In Firebase Console → Firestore Database → Click Create Database


